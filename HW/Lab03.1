#include <stdio.h>

int main(){
    int h , w ; //ประกาศตัวแปรเก็บความสูงและความกว้าง
    float area ; //ประกาศตัวแปรเก็บพื้นที่
    //----|รับค่าความสูงและความกว้าง
    printf( "h = " ) ;
    scanf( "%d" ,&h ) ;
    printf( "w = " ) ;
    scanf( "%d" ,&w ) ;
    if ( h < 0 || w < 0 ){
        printf( "\nplease enter correct number\n" ) ;
        return 0 ;
    }//end if
    
    //----|คำนวณพื้นที่สามเหลี่ยมพร้อมแสดงผลล
    printf( "Area = %.1f\n" , area = (h * w)/2.0 ) ;
    return 0 ;
}//end main
